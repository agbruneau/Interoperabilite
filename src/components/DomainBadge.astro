---
interface Props {
  domain: 'verb' | 'noun' | 'signal';
}

const { domain } = Astro.props;

const domainInfo = {
  verb: {
    label: 'Le Verbe',
    subtitle: 'Applications',
    class: 'badge-verb',
    icon: '⚡',
  },
  noun: {
    label: 'Le Nom',
    subtitle: 'Données',
    class: 'badge-noun',
    icon: '📊',
  },
  signal: {
    label: 'Le Signal',
    subtitle: 'Événements',
    class: 'badge-signal',
    icon: '📡',
  },
};

const info = domainInfo[domain];
---

<span class:list={['inline-flex items-center gap-2 px-3 py-1 rounded-full text-sm font-sans font-medium', info.class]}>
  <span>{info.icon}</span>
  <span>{info.label}</span>
</span>
